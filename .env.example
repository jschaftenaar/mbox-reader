# Mbox Reader Configuration
# Copy this file to .env and update with your settings

# Port to expose the web interface on
PORT=8080

# Path to your mbox file (relative to the data directory)
# The data directory is mounted at /data in the container
MBOX_FILE=/data/emails.mbox

# You can also specify a different data directory if needed
# DATA_DIR=/data